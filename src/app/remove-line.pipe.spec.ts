import { RemoveLinePipe } from './remove-line.pipe';

describe('RemoveLinePipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveLinePipe();
    expect(pipe).toBeTruthy();
  });
});
